<Window x:Class="ATM.Principal" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="Principal" Height="300" Width="300">
	<DockPanel LastChildFill="True">
		<Menu DockPanel.Dock="Top">
			<MenuItem Header="_Arquivo">
				<MenuItem Header="_Login" Click="Login_Click" />
				<Separator />
				<MenuItem Header="_Sair" Click="Sair_Click" />
			</MenuItem>
			<MenuItem Header="_Usuários">
				<MenuItem Header="_Ver Usuários" Click="VerUsuarios_Click" />
				<MenuItem Header="_Criar Usuário" Click="CriarUsuario_OnClick" />
				<MenuItem Header="_Remover todos usuários" Click="RemoverUsuarios_OnClick" />
			</MenuItem>
		</Menu>
		<Frame Name="frame1"></Frame>
	</DockPanel>
</Window>